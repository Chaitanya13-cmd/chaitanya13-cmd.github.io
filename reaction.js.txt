let startTime = 0;
let waiting = false;

const btn = document.getElementById("reactBtn");
const result = document.getElementById("reactResult");

btn.addEventListener("click", () => {
  if (!waiting) {
    // start game
    result.innerText = "Get ready...";
    btn.innerText = "WAIT...";
    btn.disabled = true;

    const delay = Math.random() * 3000 + 2000; // 2–5 sec

    setTimeout(() => {
      startTime = Date.now();
      waiting = true;
      btn.innerText = "CLICK NOW!";
      btn.disabled = false;
    }, delay);

  } else {
    // reaction click
    const reaction = Date.now() - startTime;
    waiting = false;

    btn.innerText = "START AGAIN";
    result.innerText = `⚡ Your reaction time: ${reaction} ms`;

    if (reaction < 200) {
      result.innerText += " 🔥 INSANE!";
    } else if (reaction < 350) {
      result.innerText += " 😎 FAST!";
    } else {
      result.innerText += " 😈 TOO SLOW!";
    }
  }
});
